{
  "scripts": {
    "dev": "concurrently \"npm run serveur\" \"npm run client\"",
    "serveur": "cd server && node server.js",
    "client": "cd client && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
